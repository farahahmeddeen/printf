#include "main.h"

/**
  *
  */
int _string(va_list zab)
{
	int f;
	char *ptr;
	int z;

	if (ptr == NULL)
		return (NULL);

	for (z = 0; ptr[i] != '\0'; z++)
		;

	f = 0;
	while (f < z)
	{
		_putchar(ptr[f]);
		f++;
	}
	return (z);
}
